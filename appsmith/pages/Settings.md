# Settings

Bind `/settings` to show the active GA4 property, backend environment, cache state, event-name mapping, and custom-definition mapping. Never expose or upload the service-account key here.

