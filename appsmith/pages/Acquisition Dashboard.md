# Acquisition Dashboard

Use the KPI object for install figures, `campaign_performance` and `traffic_sources` for tables/charts, and `countries` plus `devices` for segmentation. Uninstalls intentionally shows “Not available” until another data source is added.

