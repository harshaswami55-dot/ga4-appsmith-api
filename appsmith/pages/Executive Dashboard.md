# Executive Dashboard

Use `ExecutiveSummary.data.data.kpis` for cards and `daily_trend` for a multi-series line chart. Recommended cards: active users, new users, DAU, MAU, stickiness, engagement rate, sessions, and screen views.

