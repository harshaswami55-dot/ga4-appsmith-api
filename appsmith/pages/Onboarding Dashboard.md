# Onboarding Dashboard

Use `funnel` for the tutorial funnel, KPI values for completion/failure/skip rates, and `steps` for tutorial-step diagnostics. A GA4 custom duration metric is needed for average tutorial time.

